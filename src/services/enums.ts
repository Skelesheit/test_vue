export enum Theme {
    Light = 'neurop-light',
    Dark = 'neurop-dark',
}

export enum Language {
    Russian = 'ru',
    English = 'en',
}

export enum UserType {
    Individual = 'Физ. лицо',
    Legal = 'ИП',
    LegalEntity = 'Юр. лицо'
}